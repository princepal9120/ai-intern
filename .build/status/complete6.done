# Agent 6 — Diff Viewer: COMPLETE

## Task
Syntax-highlighted unified diff viewer, mounted in the dashboard App for
completed runs (spec/COMPLETION.md, Agent 6).

## Files
- NEW `web/src/components/DiffViewer.tsx` — required path; pre/code block,
  per-line coloring (add=green, del=red, hunk=blue, file=bold), empty-diff
  fallback ("No file changes produced").
- NEW `dashboard/components/DiffViewer.tsx` — same component local to the
  dashboard bundle (Vite root is `dashboard/`, avoids cross-root import).
- MOD `dashboard/app.tsx` (shared with Agent 5) — `extractCompletedDiff`
  helper, `diff?: string` on RetainedRun, DiffViewer mounted for completed
  live runs and completed retained runs; "No file changes produced" when a
  completed run has no diff.
- MOD `test/dashboard.test.ts` — new `renders diff output for completed runs`
  test (completed run with unified diff asserts run id, status, diff lines,
  `<pre` and `<code`).
- MOD `vitest.config.ts` (unowned shared config) — `esbuild.jsx: automatic`
  + `oxc.jsx.runtime: automatic` so `.tsx` under `web/` (nearest tsconfig is
  astro `jsx: preserve`) transforms under vitest, matching vite.config.ts
  build behavior. Fixes suite-wide transform failure also blocking Agent 5.

## Verification (TDD)
- FAIL first: `npx vitest run test/dashboard.test.ts -t "renders diff output"`
  → 1 failed (`expected ... to contain 'diff --git'`).
- After implement: target test PASS; full `test/dashboard.test.ts` 5/5 PASS.
- Full suite: 11 files, 116 tests, all PASS.
- `npm run typecheck`: clean.
- Did NOT modify wrangler.jsonc, package.json, Dockerfile.

## Note
Commit e418a6c (Agent 5, task form) landed mid-task and swept in the shared
hunks of `dashboard/app.tsx` + `test/dashboard.test.ts`; this commit adds the
remaining Agent 6 files (both DiffViewer copies, vitest config, this status).
